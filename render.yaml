services:
  - type: web
    name: novo-group-app
    env: node
    buildCommand: npm install
    startCommand: node backend/server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
